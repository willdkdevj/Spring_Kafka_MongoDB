# Default Configuration
spring.application.name=test-kafka-provider
logging.level.root=INFO
server.port=8081

# Kafka Configuration
spring.kafka.bootstrapServers=localhost:29092
spring.kafka.producer.request.topic=order-saved
spring.kafka.consumer.group-id=orders

